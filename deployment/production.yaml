---
apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: challenge-bot
  labels:
    app: challenge-bot
    role: agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: challenge-bot
      role: agent
  template:
    metadata:
      labels:
        app: challenge-bot
        role: agent
    spec:
      containers:
      - name: challenge-bot
        image: 856572510817.dkr.ecr.eu-west-1.amazonaws.com/challenge-bot:v110
        imagePullPolicy: Always
        env:
        - name: NODE_ENV
          value: production
        - name: NAHMII_BASE_URL
          value: api.nahmii.io
        - name: ETHEREUM_NODE_URL
          value: http://ethereum.hubii.com
        - name: CHALLENGE_BOT_UTCADDRESS
          value: f05179bac3d1fbef58a2fcd7ad0f769840027cc6
        - name: CHALLENGE_BOT_UTCSECRET
          valueFrom:
            secretKeyRef:
              name: mainnet-keyfile
              key: secret
        - name: CHALLENGE_BOT_APPID
          valueFrom:
            secretKeyRef:
              name: payment-engine
              key: appid
        - name: CHALLENGE_BOT_APPSECRET
          valueFrom:
            secretKeyRef:
              name: payment-engine
              key: appsecret
        resources:
          requests:
            memory: "500Mi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1"
        volumeMounts:
        - name: keystore
          mountPath: /app/keystore
      volumes:
      - name: keystore
        secret:
          secretName: mainnet-keyfile
